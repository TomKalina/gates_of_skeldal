node_modules/
dist/
coverage/
*.tsbuildinfo

# Copyrighted game assets and fixtures derived from them — NEVER commit.
# Only hash manifests of fixtures are committed (see docs in #3).
fixtures/
*.DDL
*.MAP
*.DAT
*.ENC
*.MUS

# Reference screenshots of the real game, kept locally for pixel comparison
# during porting — never commit (real game's copyrighted art/UI).
docs/reference/
